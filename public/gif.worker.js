// GIF.js worker script
// This is required for gif.js to work properly
// The library will load this worker to encode the GIF in a separate thread

// Import the gif.js worker code
importScripts("https://cdn.jsdelivr.net/npm/gif.js@0.2.0/dist/gif.worker.js")
